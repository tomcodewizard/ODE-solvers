from setuptools import setup

setup(name='core',
      version='0.1',
      author='Tom Reeds',
      packages=['core'],
      install_requires = ["numpy", "matplotlib", "scipy"],
      license='GPLv3')
